import { Sheet, Typography } from "@mui/joy";

const WeightTracker = () => {
  return (
    <div>
      <Sheet sx={{ borderRadius: "md", p: "24px" }}>
        <Typography>Weight Tracker Page</Typography>
      </Sheet>
    </div>
  );
};

export default WeightTracker;
