import { Sheet, Typography } from "@mui/joy";

const ActivityTracker = () => {
  return (
    <div>
      <Sheet sx={{ borderRadius: "md", p: "24px" }}>
        <Typography>Activity Tracker Page</Typography>
      </Sheet>
    </div>
  );
};

export default ActivityTracker;
