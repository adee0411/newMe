import { Sheet, Typography } from "@mui/joy";

const MealPlanner = () => {
  return (
    <div>
      <Sheet sx={{ borderRadius: "md", p: "24px" }}>
        <Typography>Meal Planner Page</Typography>
      </Sheet>
    </div>
  );
};

export default MealPlanner;
